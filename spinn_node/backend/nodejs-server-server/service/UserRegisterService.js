'use strict';


/**
 * Register a new user.
 * 
 *
 * body Register User's details to be registered
 * no response value expected for this operation
 **/
exports.registerUser = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

